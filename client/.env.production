# Baked into the Vite build automatically for production builds (e.g. Vercel).
# Not a secret — this is just the public backend URL.
VITE_API_BASE_URL=https://research-paper-gap-analyzer.onrender.com/api
